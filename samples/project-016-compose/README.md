# IaC samples

Infrastructure as Code is a key aspect of daily work on enterprise solutions,
being it kotlin-based or not.

## Compose samples

For docker com pose samples, you just need to enter the folder and execute the
compose command.

### Hello world

```bash
cd hello-world
docker compose up
```

### database

```bash
cd database
docker compose -f docker-compose-dev.yml up
```
